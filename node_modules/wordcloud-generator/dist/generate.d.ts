interface Options {
    puppeteerPath?: string;
    viewportWidth?: number;
    viewportHeight?: number;
    minSize?: number;
    maxSize?: number;
    backgroundColor?: string;
    font?: string;
    padding?: number;
}
/**
 *
 * @param words Array of strings or Map of strings and frequencies
 * @param normalize Normalize frequencies (recommended)
 * @param savePath Path to save image
 * @param opts Other options
 */
export declare function generate(words: string | string[] | Map<string, number>, normalize?: boolean, savePath?: string, opts?: Options): Promise<void>;
export {};
